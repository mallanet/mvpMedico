-- Optional seed for local demos.
-- Create users via Auth UI or:
--   email: doctor@example.com / password: password123
-- Bootstrap trigger creates clinic, resource, landing, membership (paused).
-- Activate membership as admin_waira or via SQL:

-- update public.memberships set status = 'active', activated_at = now();
