# === Required for Vercel / production ===
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# Server-only — never expose to the browser / never commit real values
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# Canonical public URL (no trailing slash). Used for OAuth fallback + SSR copy.
NEXT_PUBLIC_APP_URL=https://YOUR_APP.vercel.app

# === Optional ===
NEXT_PUBLIC_MALLANET_DONATION_URL=https://mallanet.example/donate

# Google Calendar OAuth (busy → block). App works without these; Google routes return 501.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Must match Google Cloud Console exactly. Defaults to ${NEXT_PUBLIC_APP_URL}/api/google/callback
GOOGLE_REDIRECT_URI=https://YOUR_APP.vercel.app/api/google/callback

# === Local overrides (copy to .env.local) ===
# NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
# NEXT_PUBLIC_APP_URL=http://localhost:3000
# GOOGLE_REDIRECT_URI=http://localhost:3000/api/google/callback

# Playwright (optional — smoke tests run without these)
# E2E_DOCTOR_EMAIL=doctor@example.com
# E2E_DOCTOR_PASSWORD=password123
# E2E_LANDING_SLUG=dra-demo
# E2E_ADMIN_EMAIL=admin@example.com
# E2E_ADMIN_PASSWORD=password123
